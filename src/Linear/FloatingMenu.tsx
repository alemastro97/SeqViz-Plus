import * as React from "react";

export default function FloatingMenu({top, left}) {
  return (
    <div className="testing-menu" style={{ position:"absolute", background:"red",top, left }}>
        djdkjddjjd
    </div>
  )
}
